"""Implement these in order. Avoid looking up completed solutions."""


def frequency_map(values: list[int]) -> dict[int, int]:
    """Return the frequency of every integer in values."""
    raise NotImplementedError


def are_anagrams(first: str, second: str) -> bool:
    """Return whether the strings contain exactly the same characters."""
    raise NotImplementedError


def can_construct(note: str, magazine: str) -> bool:
    """Return whether magazine has every character needed to build note."""
    raise NotImplementedError


def top_k_frequent(values: list[int], k: int) -> list[int]:
    """Return the k most frequent values; break ties by smaller value first."""
    raise NotImplementedError

